# =============================================================================
# AuditForecaster Environment Configuration
# =============================================================================
# Copy this file to .env and fill in your values
# Generate secrets with: openssl rand -base64 32
# =============================================================================

# =============================================================================
# REQUIRED - Application will not start without these
# =============================================================================

# Database password - use a strong random password
# Generate with: openssl rand -base64 24
POSTGRES_PASSWORD=CHANGE_ME_GENERATE_STRONG_PASSWORD

# Your production URL (where users will access the app)
# Examples: https://audit.ulrichenergyauditing.com or http://192.168.1.100:3000
NEXTAUTH_URL=https://audit.ulrichenergyauditing.com

# Authentication secret - must be at least 32 characters
# Generate with: openssl rand -base64 32
NEXTAUTH_SECRET=CHANGE_ME_GENERATE_32_CHAR_SECRET

# =============================================================================
# EMAIL - Recommended for production notifications
# =============================================================================

# Resend API key (get from https://resend.com)
RESEND_API_KEY=re_xxxxxxxxxxxx

# From address for emails (must be verified in Resend)
EMAIL_FROM="Ulrich Energy Auditing <notifications@ulrichenergyauditing.com>"

# Admin email for notifications (inspection completed, etc.)
ADMIN_NOTIFICATION_EMAIL=shaun.ulrich@ulrichenergyauditing.com

# =============================================================================
# MONITORING - Recommended for production
# =============================================================================

# Sentry DSN for error tracking (get from https://sentry.io)
NEXT_PUBLIC_SENTRY_DSN=https://xxx@xxx.ingest.sentry.io/xxx

# =============================================================================
# RATE LIMITING - Optional (app has fallback to in-memory)
# =============================================================================

# Upstash Redis (get from https://upstash.com - has free tier)
# Only needed if you want distributed rate limiting
# UPSTASH_REDIS_REST_URL=https://xxx.upstash.io
# UPSTASH_REDIS_REST_TOKEN=xxx

# =============================================================================
# GOOGLE INTEGRATION - For Calendar sync
# =============================================================================

# Google OAuth credentials
# Get from: https://console.cloud.google.com
# Project owner: shaun.ulrich@ulrichenergyauditing.com
GOOGLE_CLIENT_ID=xxx.apps.googleusercontent.com
GOOGLE_CLIENT_SECRET=GOCSPX-xxx

# =============================================================================
# WEBHOOK SECRETS - For external integrations
# =============================================================================

# SupplyPro webhook signature verification
# Generate with: openssl rand -hex 32
SUPPLYPRO_WEBHOOK_SECRET=

# Google Calendar push notification token
# Generate with: openssl rand -hex 32
GOOGLE_CALENDAR_WEBHOOK_TOKEN=

# =============================================================================
# DATA PATHS - Unraid specific (usually don't need to change)
# =============================================================================

# Base path for all persistent data
DATA_PATH=/mnt/user/appdata

# =============================================================================
# SECURITY - Testing only, NEVER enable in production
# =============================================================================

# E2E test bypass - MUST be false or unset in production
# ENABLE_E2E_AUTH_BYPASS=false
