name: Security Audit

on:
  push:
    branches: [ main, develop ]
  pull_request:
    branches: [ main, develop ]
  workflow_dispatch:

jobs:
  debug-visibility:
    name: Debug Visibility Check
    runs-on: ubuntu-latest
    steps:
      - run: echo "If you can see this, the previous YAML had a syntax error."
