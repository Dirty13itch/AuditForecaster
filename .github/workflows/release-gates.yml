name: Release Gates (AAA Blueprint)

on:
  pull_request:
    branches: [main]
  push:
    branches: [main]
  workflow_dispatch:

jobs:
  quality-gates:
    runs-on: ubuntu-latest
    timeout-minutes: 30
    
    services:
      postgres:
        image: postgres:15
        env:
          POSTGRES_USER: test_user
          POSTGRES_PASSWORD: test_password
          POSTGRES_DB: test_db
        options: >-
          --health-cmd pg_isready
          --health-interval 10s
          --health-timeout 5s
          --health-retries 5
        ports:
          - 5432:5432
    
    steps:
      - name: Checkout code
        uses: actions/checkout@v4
      
      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'
          cache: 'npm'
      
      - name: Install dependencies
        run: npm ci
      
      - name: Install Playwright browsers
        run: npx playwright install --with-deps chromium
      
      - name: TypeScript baseline check
        run: node scripts/ts-baseline-check.mjs
        # Enforces baseline: blocks NEW TypeScript errors while allowing documented tech debt
        # Current baseline: 2,290 errors (see .ts-error-baseline.json)
        # Cleanup plan documented in replit.md
      
      - name: Start application (background)
        run: |
          npm run dev &
          echo $! > .server.pid
        env:
          NODE_ENV: development
          PORT: 5000
          DATABASE_URL: postgresql://test_user:test_password@localhost:5432/test_db
          SESSION_SECRET: test-session-secret-for-ci-only-not-production
          REPL_ID: ci-test-repl
          REPLIT_DOMAINS: localhost,127.0.0.1
          ISSUER_URL: https://replit.com/oidc
      
      - name: Wait for application to be ready
        run: npx wait-on http://localhost:5000 --timeout 60000
      
      - name: Run unit tests with coverage
        run: npm run test:unit || echo "Unit tests not configured"
        continue-on-error: true
      
      - name: Run Golden Path E2E tests
        run: npm run test:e2e
        env:
          CI: true
          DATABASE_URL: postgresql://test_user:test_password@localhost:5432/test_db
          SESSION_SECRET: test-session-secret-for-ci-only-not-production
          REPL_ID: ci-test-repl
          REPLIT_DOMAINS: localhost,127.0.0.1
      
      - name: Run accessibility audits
        run: npm run test:accessibility
        env:
          CI: true
          DATABASE_URL: postgresql://test_user:test_password@localhost:5432/test_db
          SESSION_SECRET: test-session-secret-for-ci-only-not-production
          REPL_ID: ci-test-repl
          REPLIT_DOMAINS: localhost,127.0.0.1
      
      - name: Run Lighthouse performance audits
        run: npm run test:perf
        env:
          CI: true
          DATABASE_URL: postgresql://test_user:test_password@localhost:5432/test_db
          SESSION_SECRET: test-session-secret-for-ci-only-not-production
          REPL_ID: ci-test-repl
          REPLIT_DOMAINS: localhost,127.0.0.1
      
      - name: Upload test artifacts
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: quality-gate-results
          path: |
            public/gate-status.json
            ts-error-report.json
            docs/ACCESSIBILITY_AUDIT_STATUS.md
            docs/ACCESSIBILITY_AUDIT_REPORT.md
            docs/audit-results/
            playwright-report/
            coverage/
          retention-days: 30
      
      - name: Stop application
        if: always()
        run: |
          if [ -f .server.pid ]; then
            kill $(cat .server.pid) || true
            rm .server.pid
          fi
      
      - name: Verify quality gates
        run: |
          echo "📊 Quality Gate Results"
          echo "======================="
          if [ -f public/gate-status.json ]; then
            cat public/gate-status.json
          else
            echo "⚠️  Gate status not generated"
            exit 1
          fi
